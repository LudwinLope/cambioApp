import os

class Config:
    SECRET_KEY = os.environ.get("SECRET_KEY", "CAMBIA-ESTO-PRODUCCION")
    SQLALCHEMY_DATABASE_URI = os.environ.get(
        "DATABASE_URL",
        "mysql+pymysql://appuser:Cambia_Esto_Por_Un_Secreto_Largo@localhost/appdb"
    )
    SQLALCHEMY_TRACK_MODIFICATIONS = False

    # Cookies de sesión más seguras
    SESSION_COOKIE_HTTPONLY = True
    SESSION_COOKIE_SAMESITE = "Lax"
    SESSION_COOKIE_SECURE = True  # ponlo True cuando tengas HTTPS

